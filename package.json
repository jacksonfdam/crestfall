{
  "name": "crestfall",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "perft": "node --experimental-strip-types scripts/perft.ts",
    "fuzz": "node --experimental-strip-types scripts/fuzz-desync.ts"
  },
  "devDependencies": {
    "@types/three": "^0.185.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.0",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@vercel/analytics": "^2.0.1",
    "three": "^0.185.1"
  }
}
