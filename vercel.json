{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "headers": [
    {
      "comment": "Runtime configuration is fetched on load. Never cache it, so pointing the deployment at a different Supabase project takes effect on reload rather than after a cache purge.",
      "source": "/config.json",
      "headers": [{ "key": "Cache-Control", "value": "no-store" }]
    },
    {
      "comment": "Hashed filenames, so these can be cached hard.",
      "source": "/assets/(.*)",
      "headers": [{ "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }]
    }
  ]
}
